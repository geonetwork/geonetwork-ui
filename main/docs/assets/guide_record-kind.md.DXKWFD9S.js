import{_ as t,c as a,o as i,a3 as o}from"./chunks/framework.CN0HFQ1b.js";const p=JSON.parse('{"title":"Classification System","description":"","frontmatter":{"outline":"deep"},"headers":[],"relativePath":"guide/record-kind.md","filePath":"guide/record-kind.md"}'),s={name:"guide/record-kind.md"};function r(c,e,d,n,l,h){return i(),a("div",null,e[0]||(e[0]=[o('<h1 id="classification-system" tabindex="-1">Classification System <a class="header-anchor" href="#classification-system" aria-label="Permalink to &quot;Classification System&quot;">​</a></h1><p>While GeoNetwork natively supports metadata filtering by resource type, GeoNetwork-UI introduces a specialized classification logic. By analyzing indexed fields and resource types, the interface dynamically adapts the display and some logic based on three distinct categories:</p><h2 id="the-dataset-category" tabindex="-1">The &quot;Dataset&quot; Category <a class="header-anchor" href="#the-dataset-category" aria-label="Permalink to &quot;The &quot;Dataset&quot; Category&quot;">​</a></h2><p>This encompasses core data resources. It includes records typed as <code>dataset</code> (excluding maps) and <code>series</code>, as well as <code>featureCatalog</code> (when actually a dataset) instances representing physical data collections. Essentially, if the record describes a discrete spatial data unit or its schema, it is classified here.</p><h2 id="the-service-category" tabindex="-1">The &quot;Service&quot; Category <a class="header-anchor" href="#the-service-category" aria-label="Permalink to &quot;The &quot;Service&quot; Category&quot;">​</a></h2><p>This category provides a direct mapping to the service resource type. It identifies web services (such as WMS, WFS, or WMTS) that provide functional access to spatial data rather than the data files themselves.</p><h2 id="the-reuse-category" tabindex="-1">The &quot;Reuse&quot; Category <a class="header-anchor" href="#the-reuse-category" aria-label="Permalink to &quot;The &quot;Reuse&quot; Category&quot;">​</a></h2><p>This category identifies instances where data has been transformed or integrated into a new product:</p><ul><li><strong>Maps:</strong> Records where datasets have been styled, layered, and contextualized for visualization.</li><li><strong>Applications:</strong> Instances where raw data has been processed through specific logic to provide high-level functionality, such as network analysis, geocoding, or routing services.</li></ul><blockquote><p><strong>Note:</strong> Map classification is unique because it relies on two indexed fields in an exclusive manner: <code>dataset</code> and <code>document</code> records are only classified as <strong>Reuse</strong> if their <code>cl_presentationForm</code> is set to <code>mapDigital</code> or <code>mapHardCopy</code>.</p></blockquote><h1 id="impacts" tabindex="-1">Impacts <a class="header-anchor" href="#impacts" aria-label="Permalink to &quot;Impacts&quot;">​</a></h1><h2 id="search-and-results" tabindex="-1">Search and Results <a class="header-anchor" href="#search-and-results" aria-label="Permalink to &quot;Search and Results&quot;">​</a></h2><p>The search functionality in both the <strong>DataHub</strong> and the <strong>Metadata-Editor</strong> serves as the primary entry point to the catalog.</p><p>The Elasticsearch query is constructed by combining all active search filters. These filters are <strong>restrictive</strong> (using the <code>AND</code> operator) between different categories, while multiple values within a single filter are <strong>inclusive</strong> (using the <code>OR</code> operator).</p><p>The <strong>record kind</strong> is treated as one of these standard filters, following specific rules:</p><ul><li><strong>Dataset:</strong><ul><li><code>resourceType</code>: <code>dataset</code> or <code>document</code> (where <code>cl_presentationForm</code> is <strong>not</strong> a map).</li><li><code>series</code>.</li></ul></li><li><strong>Service:</strong><ul><li>Directly maps to the <code>service</code> resource type.</li></ul></li><li><strong>Reuse:</strong> The &quot;reuse only&quot; search logic is more intricate due to the dependency between <code>resourceType</code> and <code>cl_presentationForm</code>. The process involves: <ul><li>Generating a base query for all relevant reuse resource types.</li><li>Applying an explicit restrictive filter to handle the exact cross-filter logic (e.g., isolating <code>dataset</code> or <code>document</code> records specifically tagged as maps).</li></ul></li></ul><h2 id="datahub-route" tabindex="-1">DataHub Route <a class="header-anchor" href="#datahub-route" aria-label="Permalink to &quot;DataHub Route&quot;">​</a></h2><p>Routes are determined based on the Elasticsearch results, specifically utilizing the <code>resourceType</code> and <code>cl_presentationForm</code> indexed fields.</p><h2 id="datahub-sections-and-fields" tabindex="-1">DataHub Sections and Fields <a class="header-anchor" href="#datahub-sections-and-fields" aria-label="Permalink to &quot;DataHub Sections and Fields&quot;">​</a></h2><p>The distinction between sections and available fields is derived directly from the active route.</p><p>For more details on sections and fields, see <a href="./../apps/datahub-sections-fields.html">Datahub - sections and fields</a></p><h2 id="quality-score-pipeline" tabindex="-1">Quality Score Pipeline <a class="header-anchor" href="#quality-score-pipeline" aria-label="Permalink to &quot;Quality Score Pipeline&quot;">​</a></h2><p><a href="./../guide/metadata-quality.html">Metadata Quality</a> system</p><p>The pipeline also utilizes indexed fields but follows a procedural, cascading logic (from specific to generic):</p><ol><li><strong>Service:</strong> Identified via an exact match.</li><li><strong>Reuse:</strong> Identified by type, including the special case where <code>dataset</code> or <code>document</code> records are reclassified due to their <code>cl_presentationForm</code>.</li><li><strong>Dataset:</strong> Any remaining records default to this category.</li></ol><h2 id="metadata-editor" tabindex="-1">Metadata-Editor <a class="header-anchor" href="#metadata-editor" aria-label="Permalink to &quot;Metadata-Editor&quot;">​</a></h2><p>While all records are visible within the editor, only those classified as <strong>datasets</strong> are eligible for editing.</p>',27)]))}const g=t(s,[["render",r]]);export{p as __pageData,g as default};
